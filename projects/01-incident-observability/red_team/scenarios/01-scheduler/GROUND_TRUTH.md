# Ground truth (fill in)

Write a mechanism-level explanation, e.g.:

- What becomes contended? (single CPU runqueue, a global mutex, etc.)
- What path is on the critical path? (wakeup → enqueue → pick-next → context switch)
- Why does p99 amplify? (microbursts, queueing delay, lock convoy)

Keep it falsifiable: the Blue team should be able to confirm it using your evidence contract.
